var mongoose = require('mongoose');
var blobSchema = new mongoose.Schema({
    name: String,
    email: String,
    password: String,
    description: String,
    admin : Number});
mongoose.model('User',blobSchema);
   
